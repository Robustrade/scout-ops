apis:
  - name: v2-coins-get
    slo:
      error_rate:
        threshold: 0
        operator: ">"
        unit: percent
        window: 5m
        alert: true
      latency:
        threshold: 200
        operator: ">"
        unit: ms
        window: 5m
        alert: true
